/*
#include <stdio.h>

int blankstr(char *str)
{
	while(*str)
	{
		if (*str != ' ' && *str != '\t')
			return (0);
		str++;
	}
	return (1);
}

int main()
{
		printf("%d\n",blankstr("            "));
		printf("%d\n",blankstr("       N     "));
}
*/